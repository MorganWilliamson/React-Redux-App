//Card component
import React from "react";

const Card = (props) => {
    return (
        <p>Placeholder for card component.</p>
    )
};

//mapStateToProps

//add: connect, mapStateToProps
export default Card;